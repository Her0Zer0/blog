// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    markdown: {},
    site: 'https://her0zer0.github.io',
    base: '/builtbitbybitblog'
});
